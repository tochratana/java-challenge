public class Main {
    public static void main(String[] Day01) {
        // Condition
        // if statement is a control flow of statement that execute using Condition if true it will work but false it
        // not work
        String coffe = "OKOK";
        String  result = coffe == "OKOK" ? "The KOKO = coffe" : "The KOKO != coffe" ;
        System.out.println(result);


    }
}